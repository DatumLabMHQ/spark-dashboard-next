node_modules
.next
.vercel
.env*
.env*.local

# playwright
test-results/
playwright-report/

# runtime scan caches (regenerated; the committed seed is data/liquidation-events-seed.json)
.liquidation-*.json

# incremental typecheck cache, ~1MB and churns on every build
tsconfig.tsbuildinfo
